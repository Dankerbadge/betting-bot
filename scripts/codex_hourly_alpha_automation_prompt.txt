Run `./scripts/hourly_alpha_overnight.sh` once from the repository root.

After it finishes:
1. Read preflight outputs first:
- `outputs/overnight_alpha/hourly_automation_preflight_latest.json`
- `AUTOMATION_SECRETS_DNS_REFERENCE.md`
2. Read `outputs/overnight_alpha_latest.json`.
3. Open one inbox item summarizing:
- overall status (`ok`/`degraded`/`failed`/`skipped_*`),
- `pipeline_ready`, `live_ready`, and `live_blockers`,
- preflight status and first key/DNS blocker if preflight failed (`errors[0]` when available),
- each step result and duration,
- frontier refresh status with trusted/untrusted bucket counts,
- balance heartbeat source, age, and live-ready flag,
- freshness ages for history CSV, ws-state JSON, and execution journal,
- first concrete remediation action if any step failed.

Safety constraints:
- Keep this run research/dry-run only.
- Do not pass `--allow-live-orders`.
- Do not modify env secrets or risk caps.
- Do not report DNS failure unless preflight summary status is not `ready`.
